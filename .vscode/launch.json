{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "start server",
            "program": "${workspaceFolder}/bin/www"
        }
    ]
}
