extends layout
block content
  h1 register
  p.
    Please use a real email address (don't use a throwaway one)#[br]
    Your password needs to be at least #{passwordLength} characters
  if errors
    ul
      for err in errors
        li= err
    p please try again
  form(action="/register" id="register-form" method="post" enctype="multipart/form-data")
    div
      label(for="firstName") First Name
      input(type="text" name="firstName" placeholder="James" value=firstName required)
    div
      label(for="lastName") Last Name
      input(type="text" name="lastName" placeholder="Smith" value=lastName required)
    div
      label(for="email") Email Address
      input(type="email" name="email" placeholder="james.smith@example.net" value=email required)
    div
      label(for="phone") Phone Number
      input(type="tel" name="phone" placeholder="+1 206 555 0100" value=phone required)
    div
      label(for="address") Address
      input(type="text" name="address" placeholder="1 Infinite Loop, Cupertino, CA 95014 USA" value=address required)
    div
      label(for="password") Password
      input(type="password" name="password" placeholder="Password" required)
      input(type="password" name="passwordConfirm" placeholder="Confirm" required)
    div
      label(for="referral") Referral code
      input(type="text" name="referral" placeholder="Referral code" value=referral required)
    button(type="submit" form="register-form") register
  p Already have an account? #[a(href="/login") login into your account]
